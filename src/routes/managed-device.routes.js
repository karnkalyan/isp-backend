const express=require('express');const isAuthenticated=require('../middlewares/isAuthenticated');const checkAnyPermission=require('../middlewares/checkAnyPermission');const {handlers}=require('../controllers/managed-device.controller');
module.exports=prisma=>{const router=express.Router(),h=handlers(prisma);router.use((req,res,next)=>{req.prisma=prisma;next();},isAuthenticated(prisma));
 const view=checkAnyPermission(['devices_view','devices_read','olt_read','nas_read']),create=checkAnyPermission(['devices_create','olt_create','nas_create']),update=checkAnyPermission(['devices_update','olt_update','nas_update']),remove=checkAnyPermission(['devices_delete','olt_delete','nas_delete']),test=checkAnyPermission(['devices_testConnection','devices_test_connection','devices_connect','olt_read','nas_read']),maintenancePermission=checkAnyPermission(['devices_maintenance','olt_update','nas_update']),configure=checkAnyPermission(['devices_configure','interfaces_update','vlans_create','vlans_update','vlans_delete','routing_update','radius_update','olt_provision','ont_provision','bng_configure']),terminal=checkAnyPermission(['devices_terminal','olt_read']),backup=checkAnyPermission(['devices_backup','olt_update']),restore=checkAnyPermission(['devices_restore','configuration_rollback']),auditView=checkAnyPermission(['devices_audit_view','audit_log_read']),approve=checkAnyPermission(['configuration_approve']);
 router.get('/',view,h.list);router.post('/',create,h.create);router.post('/test-connection',test,h.testDraft);router.post('/status/refresh',test,h.bulkRefresh);
 router.get('/:deviceId',view,h.get);router.patch('/:deviceId',update,h.update);router.put('/:deviceId',update,h.update);router.delete('/:deviceId',remove,h.remove);
 router.post('/:deviceId/test-connection',test,h.testConnection);router.post('/:deviceId/status/refresh',test,h.refresh);router.post('/:deviceId/maintenance',maintenancePermission,h.maintenance);router.delete('/:deviceId/maintenance',maintenancePermission,h.clearMaintenance);router.post('/:deviceId/enable',update,h.enable);router.post('/:deviceId/disable',update,h.disable);
 router.get('/:deviceId/status-history',view,h.statusHistory);router.get('/:deviceId/audit-logs',auditView,h.auditLogs);router.get('/:deviceId/connection-diagnostics',auditView,h.diagnostics);router.get('/:deviceId/capabilities',view,h.capabilities);router.get('/:deviceId/capability-catalog',view,h.capabilityCatalog);router.get('/:deviceId/modules/:module',view,h.readModule);
 router.get('/:deviceId/configuration-tasks',view,h.listTasks);router.post('/:deviceId/configuration-tasks/preview',configure,h.previewTask);router.post('/:deviceId/configuration-tasks/:taskId/approve',approve,h.approveTask);router.post('/:deviceId/configuration-tasks/:taskId/execute',configure,h.executeTask);
 router.post('/:deviceId/terminal/command',terminal,h.executeCommand);
 router.get('/:deviceId/configuration',view,h.configuration);router.post('/:deviceId/configuration',configure,h.configure);router.get('/:deviceId/backups',view,h.listBackups);router.post('/:deviceId/backups',backup,h.createBackup);router.post('/:deviceId/backups/:backupId/restore',restore,h.restoreBackup);return router;};
